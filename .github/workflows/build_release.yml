name: Build and Release

on:
  push:
    paths: 
      - '**'
      - '!.github/workflows/**'
      - '.github/workflows/build_release.yml'

jobs:
  build_release:
    name: Build and Release my action
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Login GitHub Packages
        run: docker login -u bright -p ${{ secrets.GITHUB_TOKEN }} docker.pkg.github.com

#      - name: Build image
#        run: docker build . --file Dockerfile --tag docker.pkg.github.com/briranactions/hello-world-docker-action/hello-world-docker-action:1.1

#      - name: Publish image
#        run: docker push docker.pkg.github.com/briranactions/hello-world-docker-action/hello-world-docker-action:1.1

#      - name: Install image
#        run: docker pull docker.pkg.github.com/briranactions/hello-world-docker-action/hello-world-docker-action:1.1
